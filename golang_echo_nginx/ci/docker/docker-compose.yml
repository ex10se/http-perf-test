name: perf-test-golang-echo-nginx

services:
  golang:
    build:
      context: ../../
      dockerfile: ci/docker/backend/Dockerfile
    restart: unless-stopped
    volumes:
      - golang_echo_nginx:/tmp/golang
    environment:
      - DSN__RABBITMQ=${DSN__RABBITMQ}
      - SOCKET_PATH=/tmp/golang/app.sock
    networks:
      - perf-test-rmq

  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8089:80"
    volumes:
      - golang_echo_nginx:/tmp/golang
    depends_on:
      - golang
    networks:
      - perf-test-rmq

volumes:
  golang_echo_nginx:

networks:
  perf-test-rmq:
    external: true
    name: perf-test-rmq
