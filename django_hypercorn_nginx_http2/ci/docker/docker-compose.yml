name: perf-test-django-hypercorn-nginx_http2

services:
  hypercorn:
    build:
      context: ../../
      dockerfile: ci/docker/backend/Dockerfile
    restart: unless-stopped
    volumes:
      - hypercorn_http2:/tmp/hypercorn
      - ../../app/src:/app
    environment:
      - DJANGO_SETTINGS_MODULE=app.settings
      - PYTHONUNBUFFERED=1
      - DSN__RABBITMQ=${DSN__RABBITMQ}
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    networks:
      - perf-test-rmq

  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8084:80"
    volumes:
      - hypercorn_http2:/tmp/hypercorn
    depends_on:
      - hypercorn
    networks:
      - perf-test-rmq

volumes:
  hypercorn_http2:

networks:
  perf-test-rmq:
    external: true
    name: perf-test-rmq
